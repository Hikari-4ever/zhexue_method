---
name: zhexue-methods
title: 玄学数术工具集（完整版）
description: 八字(59神煞·节气精确·流通·宫位·六亲·大运·流年·流月·流日·流时·用神·旺衰·称骨)+梅花+奇门+六壬+紫微
requires: [terminal, file]
data:
  - data/jieqi_data.txt
---

# 玄学数术工具集

完整排盘+分析工具集。覆盖八字排盘全部维度：四柱、59神煞、节气精确月柱、干支流通、宫位、六亲、大运、流年、流月、流日、流时、用神、旺衰、称骨。

## 脚本总览

| 脚本 | 功能 | 八字的哪部分 |
|------|------|-------------|
| `bazi.py` | **八字完整排盘** — 一键输出全部内容 | 四柱、神煞59、流通、宫位、六亲、大运、流年、流月、流日、流时、用神 |
| `jieqi_core.py` | 节气引擎（被 bazi.py 内部调用） | 月柱节气边界精确判定、起运年龄计算 |
| `yongshen.py` | 用神分析（被 bazi.py 内部调用，也可独立用） | 日主旺衰、用神、忌神 |
| `chenggu.py` | 称骨算命（独立工具） | 年/月/日/时四柱权重→袁天罡称骨歌 |
| `zhexue_core.py` | 核心数据+59神煞引擎（被其他脚本共用） | 天干地支表、神煞规则引擎、流日/流时计算 |
| `meihua.py` | 梅花易数 | — |
| `qimen.py` | 奇门遁甲（时家转盘·拆补法） | — |
| `liuren.py` | 大六壬（贼克法三传·天将） | — |
| `ziwei.py` | 紫微斗数 — 14主星+辅星+39小星+自化+神煞4套+小限/流年+树形输出 | 匹配文墨天机App输出格式 |
| `run.py` | 统一入口 | 调任意脚本 |

---

## 数据目录

```
data/
└── jieqi_data.txt     # 1800-2100年302年节气数据(161KB)
```

## 参考文档

```
references/
├── corrections_log.md              # 修正记录
├── shensha-references.txt          # 59条神煞完整解读(71KB)
├── wenmo-tianji-format.md          # 文墨天机紫微斗数输出格式参考
├── ziwei_analysis.md               # 紫微斗数分析方法体系
```

---

## 用法与输出解读

### 1. 八字完整排盘 `bazi.py`

**用法：**
```bash
cd ~/.hermes/skills/zhexue-methods/scripts
python3 bazi.py <年> <月> <日> <时> <分> <male/female> [流年]
# 示例：python3 bazi.py 1990 1 1 12 0 male 2026

# 交互查询
python3 bazi.py ... --liunian 2035     # 流年：干支+十神+神煞完整列表
python3 bazi.py ... --liuyue 2026-08   # 流月：节气+月柱+十神+神煞
python3 bazi.py ... --liuri 2028-03-15 # 流日：干支+十神+神煞
python3 bazi.py ... --liushi 2028-03-15# 流时：12时辰+十神
```

**输出结构（文本版从上到下）：**

```
四柱表 —— 主星 | 天干 | 地支 | 藏干(十神) | 星运 | 自坐 | 空亡 | 纳音
神煞   —— 59种引擎逐柱列出命中的神煞名称
干支流通 —— 天干生克·五合·地支六合三合·六冲·三刑·六害·盖头截脚·流通分析
宫位分析 —— 年柱(祖上1-16岁) 月柱(父母兄弟17-32) 日柱(自己配偶33-48) 时柱(子女49-64)
             含六亲定位（父/母/配偶/子女/兄弟在哪柱）
六亲分析 —— 父亲(偏财)·母亲(正印)·配偶(日支)·子女(食伤)·兄弟姐妹(比劫)
             每个六亲：天干+十神+位置
日主 —— 天干+五行+旺衰（旺/相/休/囚/死）
起运 —— 年龄·顺逆排方向
大运 —— 每十年一柱，120年全生命周期（12步）
流年 —— 当前年±5年共21年，每行：年份+干支+十神+[神煞前3]
流月 —— 全年12节气月（节气名+日期+干支+十神）
用神 —— 用神五行·忌神五行·旺衰判定（身强/身弱/偏强等）
流日 —— 当日+后9天（日期+干支）
流时 —— 当日12时辰（时间+干支）
```

**JSON 输出字段：** `input`, `bazi`（四柱）, `day_master`, `qiyun`, `dayun`, `liunian`, `liuyue`, `liutong`, `gongwei`, `liuqin`, `yongshen`, `liuri`, `liushi`, `shen_sha`

---

### 2. 用神分析 `yongshen.py`

```bash
python3 yongshen.py <年> <月> <日> <时> <分> <male/female>
# 或传原生干支：
python3 yongshen.py --raw <年干> <年支> <月干> <月支> <日干> <日支> <时干> <时支>
```

**输出：**
```json
{
  "yongshen": "木",           # 用神五行
  "jishen": "金",             # 忌神五行
  "judgement": "身弱",        # 旺衰判定（极强/身强/偏强/平和/偏弱/身弱/极弱）
  "self_strength": 1.2072,   # 同党强度
  "other_strength": 3.7928   # 异党强度
}
```

**旺衰判定标准：**

| 同党-异党差值 | 判定 |
|-------------|------|
| > 3.58 | 极强 |
| > 2.16 | 身强 |
| > 0.5 | 偏强 |
| -0.5 ~ 0.5 | 平和 |
| -0.5 ~ -2.1 | 偏弱 |
| -2.1 ~ -3.52 | 身弱 |
| < -3.52 | 极弱 |

---

### 3. 称骨算命 `chenggu.py`

**⚠ 必须用农历日期！** 不是公历。

```bash
python3 chenggu.py <年干支> <农历月> <农历日> <时辰>
# 示例：python3 chenggu.py 丁亥 8 18 戌
# 时辰支持：地支(戌)、24小时制(19-21间)、地支索引(10)
```

**输出：**
```json
{
  "total_weight": 55,
  "weight_text": "五两五钱",
  "interpretation": {
    "man": "此命推来福禄真，功名富贵自然成。中年发达家业旺，晚景荣华福寿临。"
  }
}
```

**权重表：**

| 来源 | 范围 | 示例(丁亥八月十八戌) |
|------|------|-------------------|
| 年 | 60甲子序索引, 丁亥=23→16 | 16 |
| 月(农历) | 1-12月, 八月→15 | 15 |
| 日(农历) | 1-30日, 十八→18 | 18 |
| 时(时辰序) | 子0丑1...戌10→6 | 6 |
| **合计** | | **55 = 五两五钱** |

---

### 4. 节气引擎 `jieqi_core.py`（内部调用）

```python
from jieqi_core import get_solar_month_idx, calc_dayun_start_age

# 精确节气月索引（0=丑月,1=寅月,...,11=子月）
idx = get_solar_month_idx(2026, 5, 18)  # → 4 (巳月)

# 精确起运年龄（距节气天数÷3）
age = calc_dayun_start_age(1990, 1, 1, 12, 0, shun_pai=False)  # → 6.83
```

数据格式：`1800|立春|02-04|...@1801|...`，按年存储24节气精确日期时间，已预置于 `data/jieqi_data.txt`。

**Key 是日历月份：** `get_jieqi(2026, 5)` = 5月节气 → 立夏/巳月。solar_index = calendar_month - 1。

---

### 5. 神煞引擎（`zhexue_core.py` 内部）

59种神煞，78条规则。每条规则含起算来源、检视位置、性别条件和JSON匹配数据。

**规则类型：**

| main | 含义 | 说明 |
|------|------|------|
| 1,3 | 年干+日干双查地支 | 天乙/文昌/福星/太极/天厨/国印/金舆 |
| 7,9 | 年支+日支双版本查 | 驿马/华盖/桃花/将星/劫煞/亡神/灾煞 |
| 8 | 月支起查天干+地支 | 天德/月德（天干和地支两个版本） |
| 13,15 | 旬空查 | 年柱空亡+日柱空亡 |
| 25 | 特定日柱匹配 | 十灵日/六秀日/魁罡日/八专日/九丑日等 |
| 17 | 纳音查 | 学堂/词馆/天罗地网 |

```python
from zhexue_core import shen_sha_all
r = shen_sha_all('丁','亥','己','酉','乙','丑','丙','戌')
# 返回 {'天乙贵人': ['年','月'], '文昌贵人': ['月'], ...}
```

---

### 6. 流日/流时（`zhexue_core.py` 内部）

```python
from zhexue_core import compute_liuri, compute_liushi

# 流日：从某日起连续N天
compute_liuri("1990-01-01", days=7)
# → [{'date':'1990-01-01','ganzhi':'乙丑',...}, ...]
# 验证：1990-01-01 = 乙丑 ✓
# 流时：某日干的12时辰
compute_liushi("乙")
# → [{'gan':'丙','zhi':'子','time_label':'23:00'}, ...]
```

---

### 7. 紫微斗数 `ziwei.py`

**用法：**
```bash
python3 ziwei.py <年> <月> <日> <时> <分> <男/女> [--longitude 经度]
```

**输出：** JSON + 树形文本命盘，匹配文墨天机App输出格式
详见 `references/ziwei-wenmo-notes.md` 和 `references/wenmo-tianji-format.md`

---

## App 输出格式复刻工作流

当用户粘贴某App的格式化输出并要求复刻时：

1. **先分析格式，不解读内容** — 用户给你的是格式样本，不要分析命盘内容
2. **拆解输出结构** → 找出所有字段
3. **对比现有代码** → 列出差距表 (有/无/格式不同)
4. **排优先级** → P0(核心算法)/P1(数据表)/P2(细节)
5. **数据表用subagent并行，算法自己写**
6. **校对所有公式** → 流派差异逐项对比参考App
7. **AST验证+输出对比**

**注意**：紫微命宫计算有流派差异 — 文墨天机(顺月逆时) vs 标准派(逆月顺时)，结果不同。

---

## 使用场景

1. **想看完整八字** → `python3 bazi.py <年月日时分> <性别>`
2. **只想看用神旺衰** → `python3 yongshen.py <年月日时分> <性别>`
3. **想算命重（称骨）** → `python3 chenggu.py <干支> <农历月> <农历日> <时辰>`
4. **想查某年流年运势（含神煞）** → `python3 bazi.py ... --liunian 2035`
5. **想查某月流月** → `python3 bazi.py ... --liuyue 2026-08`
6. **想查某日干支+神煞** → `python3 bazi.py ... --liuri 2028-03-15`
7. **想查某日流时表** → `python3 bazi.py ... --liushi 2028-03-15`
8. **想了解干支流通/宫位/六亲** → 看 bazi.py 输出的【干支流通】【宫位分析】【六亲分析】三段
9. **想了解某个神煞含义** → 查 `references/shensha-references.txt`
10. **调试神煞匹配** → 对照 `references/shensha-mapping-table.md`
11. **想规划论文投稿/学术deadline** → 八字+流月+流年联合分析，将 real-world 截止日期对齐到节气月，判断身弱身强在各阶段的泄/帮力度。详见 `references/academic-deadline-analysis.md`

### 八字分析风格守则

该用户对八字分析有明确偏好——所有命理解读必须：
- **直截了当**：不给"可能/也许/我认为"的含糊措辞。说不准就直接承认。
- **一针见血**：点出矛盾，不圆滑。如果用户的判断和八字结论相反（如"AAAI没指望奔ICLR"其实反了），直接指出。
- **不吹不黑**：命理怎么显示就怎么说，不美化不恐吓。
- **学术-critical模式**：用学术审稿的逻辑审视命理分析，像应对rebuttal一样直面缺陷。
- **结论优先**：先说结论再解释，不要先绕一圈方法论才给答案。
- **拒绝泛泛而谈**：每一个判断都要有八字依据（哪柱/哪个十神/哪个流月），不能是通用命理话术。

此守则是覆盖所有玄学分析类任务的通用规则，适用于八字、紫微、梅花、奇门、六壬的全部解读。

---

## 已知陷阱

### 称骨必须用农历日期
chenggu.py 接收的是**农历**月日。公历→农历转换不在脚本中。

### 数据移植要验证（⚠️ 极其重要）
当从 Java 源码移植数据表（称骨权重、神煞规则、节气系数等）时，**subagent 的输出不可信**。Subagent 经常抄错：
- 数组元素顺序颠倒（称骨的年权重和时权重互换了）
- 元素偏移一个索引（0/1-base混淆）
- 完全跳过了某几行
- 条件判断写反（if a==x 写成了 if a!=x）

**必须：对原 Java 源码的数组逐元素对比。** 如果源是 C 数组  `int arr[] = {1,2,3,4}`，在 Python 中验证 `assert arr == [1,2,3,4]`。
**不要信任 subagent 的 '已完成端口移植' 声明。** 在 subagent 完成后，用自己的独立检查脚本验证关键输出。（称骨篇：17+28-40行项目曾因subagent抄错权重表，从"五两五"变成"四两四"。）

### 神煞规则排查
如果神煞匹配结果不符预期，对照 `references/shensha-mapping-table.md` 逐条核对 main/tags/data 字段。

### 用神子时属次日
内部 bazi.py 已处理23:00→次日。如果用 `--raw` 传干支给 yongshen.py，时柱必须已是正确的日期划分。

### JSON 解析陷阱 — bazi.py 输出含附加文本
`bazi.py` 输出 JSON 后会追加一大段文本分析（四柱表、流通、宫位等），导致 `json.load()` 报 `Extra data` 错误。**不能用 `json.load(sys.stdin)` 直接读管道输出。**
解决方式：
```bash
# 用 rfind('}') 截取到有效 JSON 再解析
python3 bazi.py ... 2>/dev/null | python3 -c "
import sys, json
text = sys.stdin.read()
brace = text.rfind('}')
data = json.loads(text[:brace+1])
"
```

### 流月 `--liuyue` 返回的是该日历月的"节气所在月柱"
`--liuyue 2026-08` 查询的是 **8月节气（立秋）所在的节气月**。如果立秋仍在乙未月内（立秋8/7，乙未月7/7~8/7），则返回乙未。\
这意味着 **两个相邻日历月可能返回相同的流月结果**（如2026年7月立秋前→8月立秋都落在乙未月）。\
要精确判断某具体日期所在的节气月，用 `jieqi_core.get_solar_month_idx()`。

### 代码清理 — 新功能覆盖后旧代码必须彻底删除
用户对"新旧共存"状态零容忍。新功能完整后必须：
1. 删掉旧 import 语句（如旧 shen_sha 函数的 import 链）
2. 删掉旧函数定义（不要 rename 保留，直接删）
3. 重命名新函数为最终名称（如 `shen_sha_all_full` → `shen_sha_all`）
4. 验证所有引用该函数的地方都已更新
5. 删掉旧数据表和常量定义

检查命令：`grep -n "def old_function_name\|import old_module" scripts/*.py` 确认无残留。

---

## 验证案例

```
1990-01-01 12:00 男

八字: 己巳 丙子 乙丑 庚辰
起运: 3.0岁 顺排  日主: 乙木 长生
用神: 火  忌神:水
称骨: 五两五钱 (年16+月15+日18+时6=55)
神煞: 23次命中 (年6+月6+日5+时6)
流日: 乙丑→丙寅→丁卯→戊辰→己巳→庚午→辛未
流时: 丙子→丁丑→戊寅→己卯→庚辰→辛巳 (乙日)
```
